SELECT s1.article, s1.dealer, s1.price
FROM shop AS s1
JOIN (
    SELECT article, MAX(price) AS max_price
    FROM shop
    GROUP BY article
) AS s2
  ON s1.article = s2.article
 AND s1.price   = s2.max_price;
